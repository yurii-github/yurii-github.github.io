
## Mission
Build custom html-based pages from PHP to be hosted via github pages, avoiding things like 'Jekill' etc.

## Workflow

1. start local server with ```./serve.sh```
1. write pages in pure PHP
1. deploy to github pages with ```./deploy.sh```
